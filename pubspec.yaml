name: football_picker
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  cloud_firestore: ^5.6.8
  firebase_auth: ^5.5.4
  firebase_core: ^3.13.1
  firebase_storage: ^12.4.10
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.0
  font_awesome_flutter: ^10.8.0
  image_picker: ^1.1.2
  path: ^1.9.1
  path_provider: ^2.1.5
  provider: ^6.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    
