import 'dart:ui';
import 'package:flutter/material.dart';

/// 🎨 Paleta de colores centralizada para la app Football Picker.
class AppColors {
  // 🌑 Fondos
  static const Color background = Color.fromARGB(255, 9, 29, 17);      // Fondo general oscuro
  // static const Color surface = Color.fromARGB(255, 18, 48, 32);        // Fondos de tarjetas / contenedores

  // ✅ Botones
  static const Color primaryButton = Color.fromARGB(255, 31, 200, 96);  // Botón principal (verde brillante)
  // static const Color buttonSecondary = Color.fromARGB(255, 20, 120, 70); // Botón secundario (verde apagado)
  // static const Color buttonDisabled = Color.fromARGB(255, 80, 80, 80); // Botón desactivado

  // 🖊️ Textos
  // static const Color textPrimary = Colors.white;                      // Texto principal
  // static const Color textSecondary = Color.fromARGB(255, 180, 180, 180); // Texto más tenue
  // static const Color textError = Color.fromARGB(255, 255, 100, 100);  // Texto de error

  // 🧾 Campos de entrada
  static const Color textFieldBackground = Color.fromARGB(255, 29, 69, 44);     // Fondo de los campos de texto
  //static const Color textFieldBorder = Color.fromARGB(255, 50, 100, 70); // Borde de los campos

  // ⚠️ Estados y alertas
  // static const Color error = Color.fromARGB(255, 200, 50, 50);        // Rojo de error
  // static const Color warning = Color.fromARGB(255, 255, 180, 60);     // Amarillo de advertencia
  // static const Color success = Color.fromARGB(255, 50, 200, 100);     // Verde de éxito

  // 🟢 Elementos destacados (como chips, etiquetas, etc.)
  //static const Color highlight = Color.fromARGB(255, 44, 160, 120);
}
